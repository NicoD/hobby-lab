api:
  dashboard: true
  insecure: true  # local only — disable in production

entryPoints:
  web:
    address: ":8000"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: INFO
