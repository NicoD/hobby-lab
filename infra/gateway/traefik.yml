api:
  dashboard: true
  insecure: true  # sandbox only — disable in production

entryPoints:
  web:
    address: ":80"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: INFO
