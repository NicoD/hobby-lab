api:
  dashboard: true
  insecure: true  # sandbox only — disable in production

entryPoints:
  web:
    address: ":8000"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: INFO
